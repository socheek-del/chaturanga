/** Test-only ffish helpers, shared with the other rules engines. Never bundled into the app. */
export {
  type Ffish,
  type FfishBoard,
  loadFfish,
  mulberry32,
  normalizeFen,
} from '@chaturanga/rules-core/testing';
